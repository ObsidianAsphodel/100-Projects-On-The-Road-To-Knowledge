decimalsIntoBinary = (decimals) => {
    let binary = decimals.toString(2)
    console.log("Your name converted into binary is:" + binary)
}
// toString(2) - binary representation of a number. In this case a decimal.
decimalsIntoBinary(2.1)